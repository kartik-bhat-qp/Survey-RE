# Python dependencies for the local CLI: scripts/transcripts_to_excel.py
# Install with: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Vercel Next.js functions do not run Python. The Transcripts web converter
# uses Node (exceljs) so it works on Vercel without this file.
openpyxl==3.1.5
